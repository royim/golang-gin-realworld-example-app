#!/bin/sh
#
# Pre-commit hook: runs lint + unit tests via make check.
# Install: make setup-hooks

echo "🔍 Running pre-commit checks..."
echo ""

make check
STATUS=$?

if [ $STATUS -ne 0 ]; then
    echo ""
    echo "✗ Pre-commit check failed. Fix issues before committing."
    exit 1
fi

exit 0
